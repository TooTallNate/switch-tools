# The compiled WASM artifact is GPL-3.0 (built from cnc-ra-libs source)
# and is never committed back to this repository. Users build their own.
src/bink2.wasm

# Build outputs
build/

# Cloned upstream source (fetched by `make setup-source`)
c/cnc-ra-libs/

# TS build output
dist/
