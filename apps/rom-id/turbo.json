{
	"$schema": "https://turborepo.org/schema.json",
	"extends": ["//"],
	"pipeline": {
		"build": {
			"inputs": ["app/**", "db/*", "*.config.*"],
			"outputs": [
				".next/**",
                "!.next/cache/**"
			]
		}
	}
}
